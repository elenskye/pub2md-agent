[project]
name = "pub2md-agent"
version = "0.1.0"
description = "LangGraph agent that converts multi-article PDFs into bilingual (EN + zh-CN) Markdown, one file per article"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=1.0",
    "langchain-openai>=1.0",
    "pymupdf>=1.24",
    "python-dotenv>=1.0",
    "pydantic>=2.0",
    "opencc-python-reimplemented>=0.1",
    "django>=5.0",
    "whitenoise>=6.6",
]

[project.optional-dependencies]
# Serving the app to anything other than localhost. Not needed for local
# use; `pip install -e ".[server]"` when a deployment happens again.
server = ["gunicorn>=22.0"]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings"
testpaths = ["tests", "webapp"]
pythonpath = [".", "webapp"]
# Collect Django-convention tests.py alongside pytest-convention test_*.py.
python_files = ["test_*.py", "tests.py"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["src*"]
